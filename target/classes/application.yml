server:
  port: 8050

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/code_buddy
    username: postgres
    password: industrial
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    defer-datasource-initialization: true

  sql:
    init:
      mode: always
